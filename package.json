{
  "name": "playwright-xtrace",
  "version": "1.0.0",
  "description": "X-Trace Management System — Playwright Automation Suite",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:login": "playwright test src/tests/auth/login.spec.ts --headed",
    "test:report": "playwright show-report",
    "allure:clean": "npx rimraf allure-results allure-report",
    "allure:generate": "allure generate allure-results --clean -o allure-report",
    "allure:serve": "allure serve allure-results",
    "allure:open": "allure open allure-report",
    "test:allure": "npm run allure:clean && npx playwright test && npm run allure:generate",
    "test:login:allure": "npm run allure:clean && npx playwright test src/tests/auth/login.spec.ts && npm run allure:generate"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0",
    "allure-commandline": "^2.42.1",
    "allure-playwright": "^3.10.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.4.5"
  }
}
